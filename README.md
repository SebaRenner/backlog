# Backlog

## Edge function secrets

After deploying the edge function (`pnpm fn:deploy`), set the required secrets once:

```
supabase secrets set GHP_TOKEN=<github-pat>
supabase secrets set APP_PASSWORD=<app-password>
```
